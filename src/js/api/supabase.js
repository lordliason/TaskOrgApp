/**
 * Supabase client setup and configuration.
 * TODO: Extract from index.html when migrating.
 */

// Placeholder - will be populated during migration
export const supabase = window.supabase || null;
